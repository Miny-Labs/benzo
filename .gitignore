# --- secrets (never commit) ---
.env
.env.*
!.env.example

# --- node / ts ---
node_modules/
.pnpm-store/
dist/
build/
*.tsbuildinfo

# --- hardhat / solidity ---
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# --- zk artifacts (large / generated; rebuild with hardhat zkit) ---
zkit/
generated-types/
*.zkey
*.ptau
*.wtns
*.r1cs

# --- os / editor ---
.DS_Store
*.log
.idea/
.vscode/

# vendored reference material (present locally, not committed)
reference/

# retired Stellar-era local material (untracked leftovers; tracked code lives at fbb4d4e)
ceremony/
test-results/
target/

# --- agent / tooling internal (kept locally, not part of the codebase) ---
.claude/
CLAUDE.md
AGENTS.md
.impeccable.md
.mcp.json
.neon

# --- internal planning / marketing / demo docs (kept locally for context) ---
docs/

# --- heavy binary doc artifacts (regenerable) ---
*.pptx
*.pdf
.vercel
