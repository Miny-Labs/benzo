{
  "name": "benzo-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Benzo: private-by-default shielded-USDC payments protocol on Stellar (Soroban)",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "audit:prod-env": "node scripts/audit-production-env.mjs",
    "audit:prod-db": "node scripts/audit-production-db.mjs",
    "audit:privacy": "node scripts/audit-privacy-payloads.mjs",
    "audit:actions": "node scripts/audit-action-coverage.mjs",
    "smoke:wallet-account-freedom": "node scripts/smoke-wallet-account-freedom.mjs",
    "test": "pnpm -r test",
    "lint": "biome lint",
    "lint:fix": "biome lint --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0"
  }
}
